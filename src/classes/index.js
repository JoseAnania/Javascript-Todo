//Archivo que contendrá las referencias a todas las clases, copiamos las que se van creando automáticamente y pegamos aquí (Ojo con directorio que hay que cambiarlo)

import { Todo } from './todo.class'; 
import { TodoList } from './todo-list.class'; 

export {
    Todo,
    TodoList,
}